---
id: REQ-001
title: Python environment & MLX installation
status: completed
created_at: 2026-03-09T12:00:00Z
claimed_at: 2026-03-09T12:10:00Z
route: A
completed_at: 2026-03-09T12:15:00Z
user_request: UR-001
source_step: "1.1"
---

# Python Environment & MLX Installation

## What
Set up a clean Python virtual environment and install MLX with its LLM tooling for fine-tuning on the M1 iMac.

## Context
Refers to TODO step 1.1. The M1 iMac has Python and Homebrew installed. MLX is probably not installed. We want an isolated environment to avoid dependency conflicts.

## Requirements
- Create a virtual environment: `python3 -m venv .venv`
- Activate it and upgrade pip
- Install: mlx, mlx-lm, huggingface_hub
- Create a requirements.txt capturing installed versions
- Update .gitignore (add .venv/, models/, __pycache__/)

## Test Criteria
- `python -c "import mlx.core as mx; print(mx.ones(3))"` prints a valid array
- `python -c "import mlx_lm; print('mlx_lm OK')"` runs without errors

---
*Source: step 1.1*

## Verification

**Source**: UR-001/input.md
**Pre-fix coverage**: 100% (5/5 items)

### Coverage Map

| # | Item | REQ Section | Status |
|---|------|-------------|--------|
| 1 | Create virtual environment (.venv) | Requirements | Full |
| 2 | Install mlx, mlx-lm, huggingface_hub | Requirements | Full |
| 3 | Create requirements.txt | Requirements | Full |
| 4 | Update .gitignore (.venv/, models/, __pycache__/) | Requirements | Full |
| 5 | TEST: Verify imports work | Test Criteria | Full |

*Verified by verify-request action*

---

## Triage

**Route: A** - Simple

**Reasoning:** Well-defined setup task with specific commands. All steps are named: create venv, install packages, create requirements.txt, update .gitignore. No discovery needed.

## Plan

1. Run `python3 -m venv .venv` in project root, activate, upgrade pip
2. Install `mlx mlx-lm huggingface_hub` via pip, then `pip freeze > requirements.txt`
3. Create/update `.gitignore` with `.venv/`, `models/`, `__pycache__/`
4. Test: run the two import verification commands

*Generated by work action (Route A — inline plan)*

## Plan Verification

**Source**: REQ-001 (5 items enumerated)
**Pre-fix coverage**: 100% (5/5 items addressed)

### Coverage Map

| # | Requirement | Plan Step | Status |
|---|------------|-----------|--------|
| 1 | Create virtual environment (.venv) | Step 1 | Full |
| 2 | Install mlx, mlx-lm, huggingface_hub | Step 2 | Full |
| 3 | Create requirements.txt | Step 2 | Full |
| 4 | Update .gitignore | Step 3 | Full |
| 5 | Verify imports work | Step 4 | Full |

*Verified by verify-plan action*

## Exploration

**Not needed** -- plan names specific files and changes. No discovery required.

*Skipped by work action*

## Implementation Summary

- Created `.venv/` — Python 3.10.11 virtual environment, pip upgraded to 26.0.1
- Installed: mlx 0.31.0, mlx-lm 0.31.0, huggingface_hub 1.6.0, transformers 5.3.0 (35 packages total)
- Created `requirements.txt` with pinned versions
- Updated `.gitignore` — added `models/` (`.venv/` and `__pycache__/` already present)

*Completed by work action (Route A)*

## Testing

**Tests run:** Python import verification commands
**Result:** All tests passing (2 tests)

- `import mlx.core as mx; print(mx.ones(3))` → `array([1, 1, 1], dtype=float32)`
- `import mlx_lm; print('mlx_lm OK')` → `mlx_lm OK`

**No testing infrastructure detected** — project has no test framework yet (expected at this stage).

*Verified by work action*
