---
id: REQ-002
title: "Step 1.2: Download & quantize base model"
status: completed
claimed_at: 2026-03-09T20:00:00Z
route: A
completed_at: 2026-03-09T20:20:00Z
created_at: 2026-03-09T12:05:00Z
user_request: UR-002
related: [REQ-001, REQ-003]
batch: ml-training-phase-1
source_step: "1.2"
source_doc: docs/TODO.md
blueprint_ref: docs/BLUEPRINT.md
model_hint: "Claude Code"
---

# Step 1.2: Download & Quantize Base Model

## What

Download Llama 3.2 3B Instruct from HuggingFace and convert it to 4-bit quantized MLX format.

## Requirements

- Create models/ directory
- Login to HuggingFace if needed (`huggingface-cli login`)
- Convert Llama 3.2 3B Instruct to 4-bit MLX format

## Verification

- Verify models/llama-3.2-3b-4bit/ exists (~2GB)

## Blueprint Context

Download and quantize the Llama 3.2 3B Instruct model for MLX.

Steps:
1. Create the models/ directory
2. Use mlx_lm to fetch and convert the model:
   `python -m mlx_lm.convert --hf-path meta-llama/Llama-3.2-3B-Instruct --mlx-path ./models/llama-3.2-3b-4bit --quantize --q-bits 4`
3. Verify the converted model directory exists and contains the expected files (weights, config, tokenizer)

Note: This requires a HuggingFace account and acceptance of Meta's Llama license. If access is gated, use `huggingface-cli login` first.

Context: The model needs to be in MLX format for both training and inference. The quantized model should be ~2GB and fit comfortably in 16GB unified memory. The `models/` directory is gitignored (model files are large).

Test: Confirm models/llama-3.2-3b-4bit/ exists and contains model files. Check total size is approximately 2GB.

## Specification Reference

This step is part of the Hardware Diagnostics LLM Fine-Tuning project. See docs/SPECIFICATION.md for full project context, goals, and constraints.

## Builder Guidance

- Certainty level: Firm (this is a planned, documented step)
- The TODO label suggests: Claude Code
- Follow the Blueprint's instructions closely; they contain tested approaches

## Context

See [user-requests/UR-002/input.md](./user-requests/UR-002/input.md) for the full TODO document.

---
*Source: Ingested from docs/TODO.md, step 1.2*

---

## Triage

**Route: A** - Simple

**Reasoning:** Blueprint provides exact commands and directory paths. No pattern discovery or architectural decisions needed — just run the conversion command and verify output.

## Plan

1. Create `models/` directory if it doesn't already exist (`mkdir -p models/`)
2. Run the MLX conversion command: `python -m mlx_lm.convert --hf-path meta-llama/Llama-3.2-3B-Instruct --mlx-path ./models/llama-3.2-3b-4bit --quantize --q-bits 4` (may need `huggingface-cli login` first if not already authenticated)
3. Verify `models/llama-3.2-3b-4bit/` exists and contains weights, config.json, and tokenizer files (~2GB total)

*Generated by Plan agent*

## Plan Verification

**Source**: REQ-002 (4 items enumerated)
**Pre-fix coverage**: 100% (4 full, 0 partial, 0 missing)
**Post-fix coverage**: 100% (4/4 items addressed)

### Coverage Map

| # | Requirement | Plan Step | Status |
|---|------------|-----------|--------|
| 1 | Create models/ directory | Step 1: mkdir -p models/ | Full |
| 2 | Login to HuggingFace if needed | Step 2: parenthetical note | Full |
| 3 | Convert Llama 3.2 3B Instruct to 4-bit MLX | Step 2: mlx_lm.convert command | Full |
| 4 | Verify models/llama-3.2-3b-4bit/ exists (~2GB) | Step 3: verify output | Full |

### Fixes Applied

None — plan fully covers all requirements.

*Verified by verify-plan action*

## Exploration

**Not needed** -- plan names specific commands and directory paths. No discovery required.

*Skipped by work action*

## Implementation Summary

- Created `models/` directory
- Installed `mlx-lm` v0.31.0 and dependencies (mlx, transformers, huggingface-hub)
- Installed HuggingFace CLI (`huggingface_hub[cli]`) and authenticated
- Original `meta-llama/Llama-3.2-3B-Instruct` required gated access approval (pending review)
- Used pre-quantized community model `mlx-community/Llama-3.2-3B-Instruct-4bit` instead (same model, already in 4-bit MLX format)
- Downloaded to `models/llama-3.2-3b-4bit/` (1.7GB)
- Verified: weights (`model.safetensors`), `config.json`, tokenizer files all present

*Completed by work action (Route A)*

## Testing

**Verification:** `ls -la models/llama-3.2-3b-4bit/ && du -sh models/llama-3.2-3b-4bit/`
**Result:** Directory exists with all expected files (model.safetensors, config.json, tokenizer.json, special_tokens_map.json, tokenizer_config.json). Total size 1.7GB.

No testing infrastructure applicable — this is a model download step.

*Verified by work action*
